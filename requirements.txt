# Everything this project needs.
# Install with:  pip install -r requirements.txt

anthropic>=1.0.0     # Claude - both agents talk to the API through this
requests>=2.31.0     # plain HTTP, used for Reddit's official API
pydantic>=2.0.0      # describes the shape of the final report
